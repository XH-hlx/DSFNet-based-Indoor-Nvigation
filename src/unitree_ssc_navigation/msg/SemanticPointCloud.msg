# Semantic point cloud with labels
Header header

# Point cloud with semantic labels
sensor_msgs/PointCloud2 cloud  # xyz points
uint8[] labels                 # Per-point semantic labels
float32[] costs                # Per-point traversal costs
